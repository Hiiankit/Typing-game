/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
  },
  theme: {
    fontfamily: ["Red Hat Display", sans - serif],
  },
};

export default config;
